import { Icon } from "../../../../../shared/Icon";
import css from "./IconCalendar.module.css";

const IconCalendar = () => {
  return (
    <Icon id="icon-calendar" className={css.icon} width={18} height={18} />
  );
};

export default IconCalendar;
